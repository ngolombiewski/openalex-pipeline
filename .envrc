dotenv

# Dagster instance history is disposable; canonical configuration is tracked
# at the repository root and linked into the absolute instance directory.
export DAGSTER_HOME="$PWD/.dagster"
mkdir -p "$DAGSTER_HOME"
ln -sfn ../dagster.yaml "$DAGSTER_HOME/dagster.yaml"
